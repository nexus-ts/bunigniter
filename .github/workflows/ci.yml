name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14

      - name: Install system dependencies (sharp)
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq libvips-dev

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Create bunigniter package link for examples
        run: |
          mkdir -p node_modules/bunigniter
          ROOT=${{ github.workspace }}
          ln -sf $ROOT node_modules/bunigniter/core
          ln -sf $ROOT node_modules/bunigniter

      - name: Lint check
        run: bun run lint

      - name: Run tests
        run: bun x vitest run --reporter=verbose
        timeout-minutes: 5
